/**
 * SortBot assistant: local + optional remote LLM, session-scoped state.
 * Import from `@/components/chatbot` for the stable public API.
 */
export { processMessage } from './processMessage';
export { createAssistantSession } from './session';
